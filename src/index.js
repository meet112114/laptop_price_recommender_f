import React from 'react';
import ReactDOM from 'react-dom/client';
import LaptopPricePredictor from './App';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <LaptopPricePredictor />
  </React.StrictMode>
);

